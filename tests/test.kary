def x = (mul 11 (sum 5 6))
def y = (mul 34 (sum 5 (foo-bar 4 5)))
def m n k
con f = (and on off)
con number255 = 0xff

(console/log (div x m))
(foo-bar)

export def man-man:
    def second-level:
        def last = (sum 4 5)
        throw 0
    end
    def something = 5
end

async fun a b c:
    def result = (sum a (mul b c))
    return 1
end

(await (fun 3 4 5))